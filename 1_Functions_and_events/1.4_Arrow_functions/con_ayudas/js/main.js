// HELLO, MY NAME IS
// Ejercicio para practicar
// funciones tipo arrow

// En este ejercicio vamos a añadir nuestro nombre a la etiqueta, pero usando las funciones arrow

// lo primero es declarar las contantes   que llamen a los elementos que vamos a necesitar:
// el input y el texto donde aparecerá el nobre

// Una vez tenemos las constantes listas, necesitamos recoger la información del input en una variable.

// Pero esta variable necesita cambiar cada vez que escribamos, por lo que necesitamos que se actualice
// para ello crearemos un evento keyup que llame a una función que cambie el valor de text por
// el contenido del input. Esta fución la realicaremos en formato arrow.
// Para ello creamos una constante que llamaremos newtext, esta será el nombre de la función,
// seguido de un =, añadimos parentesis para que el sepamos qeu es una función, aunque no necesitamos
// ningún parametro para este caso, seguido de la flecha y abriendo llaves le decimos que
// text sea el nuevo value de input.

// Ahora que tememos guardado lo que escribamos en el input dentro de una variable, ahora
// Tenemos que pintar esa variable en en el HTML del nombre.

// No olvidemos añadir el eventListener con 'keyup'
